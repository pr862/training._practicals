{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "Quiz Application with Firebase Realtime Database",
  "main": "src/client.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && npm run build:css",
    "build:css": "tailwind -i ./src/styles/style.css -o ./src/styles/output.css",
    "dev": "tsc --watch",
    "dev:css": "tailwind -i ./src/styles/style.css -o ./src/styles/output.css --watch",
    "start": "npm run build && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.2.2",
    "autoprefixer": "^10.4.24",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "express": "^5.2.1",
    "firebase": "^12.9.0"
  }
}
